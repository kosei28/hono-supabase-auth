{
    "scripts": {
        "dev": "bun run --hot src/index.tsx",
        "build": "bun build src/index.tsx --outdir build"
    },
    "dependencies": {
        "@supabase/ssr": "^0.0.10",
        "hono": "^3.10.3"
    },
    "devDependencies": {
        "bun-types": "^1.0.7"
    }
}
